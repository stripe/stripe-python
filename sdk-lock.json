{
  "runtime": {
    "minVersion": "3.9",
    "commands": [
      {
        "name": "python3",
        "versionFlag": "--version",
        "versionPattern": "Python (\\d+\\.\\d+\\.\\d+)"
      },
      {
        "name": "python",
        "versionFlag": "--version",
        "versionPattern": "Python (\\d+\\.\\d+\\.\\d+)"
      }
    ]
  },
  "recipes": {
    "format": {
      "commands": [
        "[ -d .venv ] || ( python -m venv .venv && .venv/bin/python -I -m pip install -e . --quiet --disable-pip-version-check )",
        "python -I -m pip install -r deps/dev-requirements.txt --disable-pip-version-check --quiet",
        "ruff format . --quiet"
      ],
      "sourceHash": "sha256:b59ff1a2e8e8f6b19971211312f25dbd1e43cd0024f5f7c1d6ca2cdbe1505ccd"
    },
    "build": {
      "commands": [
        "[ -d .venv ] || ( python -m venv .venv && .venv/bin/python -I -m pip install -e . --quiet --disable-pip-version-check )",
        "python -I -m pip install -r deps/build-requirements.txt --disable-pip-version-check --quiet",
        "rm -rf dist",
        "python -m build",
        "python -m twine check dist/*"
      ],
      "sourceHash": "sha256:b59ff1a2e8e8f6b19971211312f25dbd1e43cd0024f5f7c1d6ca2cdbe1505ccd"
    }
  }
}
