+from _typeshed import Incomplete
+from stripe import util as util
 from stripe.api_resources.abstract.api_resource import APIResource as APIResource
-from typing_extensions import Self
+from stripe.six.moves.urllib.parse import quote_plus as quote_plus
 
 class UpdateableAPIResource(APIResource):
 
     @classmethod
-    def modify(cls, sid: str, **params) -> Self:
+    def modify(cls, sid, **params):
         ...
 
-    def save(idempotency_key: str | None, self=None) -> Self:
+    def save(idempotency_key: Incomplete | None, self=...):
         ...
